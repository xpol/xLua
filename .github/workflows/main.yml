name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Check environment variables
      run: |
        export
        find $ANDROID_SDK_ROOT -name ndk-build
      
